<br>
<h3 class="text-center">
  Tareas TikiTii completadas
</h3>
<hr>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Personas</th>
      <th>Tareas</th>
    </tr>
  </thead>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.user.email %></td>
        <td><%= task.todo.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', root_path %>